bundle:
  name: governed_airline_pipeline

include:
  - resources/*.yml

variables:
  catalog:
    description: Unity Catalog catalog used by the target environment.
  schema:
    description: Schema that contains the project objects.
    default: airline_luis_acuna
  student_name:
    description: Student identifier required by the pipeline rubric.
    default: luis_acuna
  notification_email:
    description: Email used for Lakeflow Job notifications.
    default: luis.acuna11@ulatina.net
  source_table:
    description: Read-only Airline Performance Marketplace source table.
    default: databricks_airline_performance_data.v01.flights_small
  row_count_threshold:
    description: Minimum number of rows required in the Gold table.
    default: "1"

targets:
  development:
    mode: development
    default: true
    workspace:
      host: https://dbc-cbc2bb58-ee6e.cloud.databricks.com
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab_lab_dev
    resources:
      pipelines:
        airline_pipeline:
          development: true

  production:
    mode: production
    workspace:
      host: https://dbc-cbc2bb58-ee6e.cloud.databricks.com
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab_lab_prod
    resources:
      pipelines:
        airline_pipeline:
          development: false
